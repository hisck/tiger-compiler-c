/* verify if a substring exists, print true if such substring exists otherwise false */


let 
    var str := "String1 subString1 Strinstrnd subStr ing1subString"
    var sub := "subString"
    var i:= 0
    var j:= 0
    var flag:=0
    var p1 := ""
    var p2 := ""
    var p3 := ""

    var buffer := getchar()

    function strlen(stringT: string):int =
        let
          var len:=0
        in
           while buffer
             do (len := len + 1; buffer := getchar());
            len
        end
        
in

  p1 := str;
  p2 := sub;

  for i:=0 to strlen(str) do 
    (
        if(p1 = p2) then
            p3 = p1;
            for j:=0 to strlen(sub) do
            (
                if(p3 = p2) then
                    p3 := p3 + 1;
                    p2 := p2 + 1;
            )
            if(j = strlen(sub))then
                flag := 1
                print("Is substring");
            p1 := p1 + 1;

    )
  if(flag = 0) then print("Is not substring");

end